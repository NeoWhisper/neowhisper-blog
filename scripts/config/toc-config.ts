export type TocConfig = {
  excludedHeadings: string[];
};

export const tocConfig: TocConfig = {
  "excludedHeadings": [
    "table of contents",
    "目次",
    "المحتويات",
    "references",
    "参考リンク",
    "المراجع"
  ]
};
