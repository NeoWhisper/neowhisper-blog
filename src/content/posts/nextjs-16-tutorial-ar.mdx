---
title: "بناء أول مدونة لك باستخدام Next.js 16 و App Router و MDX"
date: "2026-01-13"
excerpt: "دليل شامل لإنشاء مدونة حديثة وعالية الأداء باستخدام Next.js 16 App Router، و MDX لإدارة المحتوى، و Tailwind CSS للتصميم. مثالي للمطورين المتطلعين لبناء مشروعهم الأول في Next.js."
category: "Next.js"
coverImage: "/images/nextjs-16-tutorial-cover.png"
author:
  name: "NeoWhisper"
  picture: "/images/author.png"
---

هل ترغب في بناء مدونة؟ في عام 2026، نحن لا نكتفي بوضع HTML على خادم. نحن نبحث عن السرعة، تحسين محركات البحث الممتاز (SEO)، وتجربة مطور لا تجعلنا نبكي.

هنا يأتي دور **Next.js 16**.

هذا ليس مجرد "أهلاً بالعالم". هذا هو كيف تبني مدونة بمستوى الإنتاج باستخدام App Router و MDX و Tailwind CSS.

<Callout type="info">
  **ملاحظة الإصدار**: يستخدم هذا الدليل Next.js 16 (LTS). إذا كنت تستخدم Next.js
  17+، راجع [دليل الترقية الرسمي](https://nextjs.org/docs/app/guides/upgrading).
</Callout>

---

## لماذا Next.js 16؟

إذا كنت مطوراً، فأنت تريد السيطرة الكاملة. يمنحك Next.js تلك السيطرة مع افتراضيات أداء تتحدى الفيزياء.

1.  **مكونات الخادم (Server Components)**: يتم تقديم تدويناتك على الخادم. صفر جافا سكريبت على جانب العميل للمحتوى نفسه.
2.  **App Router**: تخطيطات بديهية وحالات تحميل واضحة باستخدام مجلدات بسيطة.
3.  **تحسين مدمج**: يتم تحسين الصور والخطوط تلقائياً.
4.  **TypeScript**: دعم من الدرجة الأولى. لا مزيد من صداع "undefined is not a function".

---

<Step number="١" title="تهيئة المشروع">

أولاً، تأكد من أن لديك Node.js 18.18 أو أحدث. افتح الطرفية:

```bash
npx create-next-app@latest neowhisper-blog
```

اختر هذه الخيارات عند المطالبة:

- **TypeScript?** Yes (دائماً)
- **Tailwind CSS?** Yes (معياري في 2026)
- **App Router?** Yes
- **Import Alias?** Yes (`@/*`)

</Step>

<Step number="٢" title="إضافة دعم MDX">

نريد بساطة Markdown مع قوة React. ثبّت حزم MDX:

```bash
npm install @next/mdx @mdx-js/loader @mdx-js/react gray-matter date-fns
npm install -D @types/mdx
```

ثم قم بتهيئة `next.config.js`:

```js
import createMDX from "@next/mdx";

/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ["js", "jsx", "md", "mdx", "ts", "tsx"],
};

const withMDX = createMDX({
  options: {
    remarkPlugins: [],
    rehypePlugins: [],
  },
});

export default withMDX(nextConfig);
```

وأنشئ `mdx-components.tsx` في المجلد الرئيسي:

```tsx
import type { MDXComponents } from "mdx/types";

export function useMDXComponents(components: MDXComponents): MDXComponents {
  return {
    h1: ({ children }) => (
      <h1 className="text-4xl font-bold mb-4">{children}</h1>
    ),
    h2: ({ children }) => (
      <h2 className="text-3xl font-bold mb-3 mt-8">{children}</h2>
    ),
    a: ({ href, children }) => (
      <a href={href} className="text-purple-600 hover:underline">
        {children}
      </a>
    ),
    ...components,
  };
}
```

</Step>

<Step number="٣" title="هيكلة المحتوى">

حافظ على نظافة الكود. خزن تدويناتك في `src/content/posts`.

```text
src/
├── app/              # المسارات
├── components/       # مكونات الواجهة
├── content/          # ملفات MDX
│   └── posts/
│       └── first-post.mdx
└── lib/              # الأدوات المساعدة
    └── posts.ts
```

أنشئ تدوينتك الأولى: `src/content/posts/first-post.mdx`:

```mdx
---
title: "تدوينتي الأولى"
date: "2026-01-13"
excerpt: "أهلاً بالعالم!"
coverImage: "/images/first-post.png"
---

# أهلاً بالعالم

هذه تدوينتي الأولى.
```

</Step>

<Step number="٤" title="بناء طبقة البيانات">

أنشئ `src/lib/posts.ts` لقراءة ملفاتك. في App Router، يمكنك جلب البيانات مباشرة—لا حاجة لحالة معقدة.

```tsx
import fs from "fs";
import path from "path";
import matter from "gray-matter";

const postsDirectory = path.join(process.cwd(), "src/content/posts");

export type Post = {
  slug: string;
  title: string;
  date: string;
  excerpt: string;
  coverImage: string;
  content: string;
};

export async function getAllPosts(): Promise<Post[]> {
  const fileNames = fs.readdirSync(postsDirectory);
  const allPostsData = fileNames
    .filter((fileName) => fileName.endsWith(".mdx"))
    .map((fileName) => {
      const slug = fileName.replace(/\.mdx$/, "");
      const fullPath = path.join(postsDirectory, fileName);
      const fileContents = fs.readFileSync(fullPath, "utf8");
      const { data, content } = matter(fileContents);

      return {
        slug,
        content,
        title: data.title,
        date: data.date,
        excerpt: data.excerpt,
        coverImage: data.coverImage,
      } as Post;
    });

  return allPostsData.sort((a, b) => (a.date < b.date ? 1 : -1));
}

export async function getPostBySlug(slug: string): Promise<Post | null> {
  try {
    const fullPath = path.join(postsDirectory, `${slug}.mdx`);
    const fileContents = fs.readFileSync(fullPath, "utf8");
    const { data, content } = matter(fileContents);

    return {
      slug,
      content,
      title: data.title,
      date: data.date,
      excerpt: data.excerpt,
      coverImage: data.coverImage,
    } as Post;
  } catch {
    return null;
  }
}
```

</Step>

<Step number="٥" title="ربط الواجهة">

في `src/app/page.tsx`، اجلب تدويناتك واعرضها:

```tsx
import { getAllPosts } from "@/lib/posts";
import ArticleCard from "@/components/ArticleCard";

export default async function Home() {
  const posts = await getAllPosts();

  return (
    <main className="container mx-auto px-4 py-12">
      <h1 className="text-4xl font-bold mb-8">آخر الأفكار</h1>
      <div className="grid md:grid-cols-2 gap-8">
        {posts.map((post) => (
          <ArticleCard key={post.slug} post={post} />
        ))}
      </div>
    </main>
  );
}
```

للتدوينات الفردية (`src/app/blog/[slug]/page.tsx`)، استخدم `generateStaticParams` لتوليدها بشكل ثابت عند البناء:

```tsx
import { getPostBySlug, getAllPosts } from "@/lib/posts";
import { notFound } from "next/navigation";
import { MDXRemote } from "next-mdx-remote/rsc";

export async function generateStaticParams() {
  const posts = await getAllPosts();
  return posts.map((post) => ({ slug: post.slug }));
}

export async function generateMetadata({
  params,
}: {
  params: { slug: string };
}) {
  const post = await getPostBySlug(params.slug);
  if (!post) return { title: "Post Not Found" };
  return { title: post.title, description: post.excerpt };
}

export default async function BlogPost({
  params,
}: {
  params: { slug: string };
}) {
  const post = await getPostBySlug(params.slug);
  if (!post) notFound();

  return (
    <article className="max-w-3xl mx-auto py-12 px-4">
      <h1 className="text-5xl font-bold mb-4">{post.title}</h1>
      <time className="text-gray-500 block mb-8">{post.date}</time>
      <div className="prose lg:prose-xl dark:prose-invert max-w-none">
        <MDXRemote source={post.content} />
      </div>
    </article>
  );
}
```

</Step>

---

## إلى أين نذهب من هنا؟

<Checklist>
  <CheckItem>**الوضع الليلي**: أضف `next-themes`.</CheckItem>
  <CheckItem>**البحث**: طبق `fuse.js` للبحث من طرف العميل.</CheckItem>
  <CheckItem>
    **تمييز الكود**: أضف `rehype-pretty-code` أو `rehype-highlight`.
  </CheckItem>
  <CheckItem>**SEO**: أضف `sitemap.xml` و `robots.txt`.</CheckItem>
  <CheckItem>**النشر**: ارفع على Vercel.</CheckItem>
</Checklist>

---

**هل تحتاج مساعدة في بناء مشروع Next.js؟** في NeoWhisper، نتخصص في تطوير الويب الحديث. تحقق من [خدماتنا](/services?lang=ar) أو [تواصل معنا](/contact?lang=ar).
