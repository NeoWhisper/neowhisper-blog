---
title: "App RouterとMDXで作る、初めてのNext.js 16ブログ"
date: "2026-01-13"
excerpt: "Next.js 16のApp Router、コンテンツ管理のためのMDX、そしてスタイリングにTailwind CSSを使用した、モダンでパフォーマンスの高いブログ作成の包括的ガイド。初めてのNext.jsプロジェクトに最適です。"
category: "Next.js"
coverImage: "/images/nextjs-16-tutorial-cover.png"
author:
  name: "NeoWhisper"
  picture: "/images/author.png"
---

ブログを作りたいですか？2026年現在、ただサーバーにHTMLを置くだけでは不十分です。スピード、SEO、そして何より「開発体験」が重要です。

そこで **Next.js 16** の出番です。

これは単なる「Hello World」ではありません。App Router、MDX、Tailwind CSSを使用して、商用レベルのブログを構築する方法を解説します。

<Callout type="info">
  **バージョンに関する注意**: このチュートリアルではNext.js
  16（LTS）を使用しています。Next.js
  17以降を使用している場合は、[公式アップグレードガイド](https://nextjs.org/docs/app/guides/upgrading)を確認してください。
</Callout>

---

## なぜ Next.js 16 なのか？

開発者なら、完全なコントロールを求めるはずです。Next.jsは、物理法則を無視するようなパフォーマンスとともに、そのコントロールを提供します。

1.  **Server Components**: ブログ記事はサーバーでレンダリングされます。コンテンツのためのクライアントサイドJavaScriptはゼロです。
2.  **App Router**: フォルダーを使った直感的なルーティングとレイアウト管理。
3.  **組み込みの最適化**: 画像やフォントは自動的に最適化されます。
4.  **TypeScript**: ファーストクラスのサポート。「undefined is not a function」とはおさらばです。

---

<Step number="1" title="プロジェクトの初期化">

まず、Node.js 18.18以降がインストールされていることを確認してください。ターミナルを開きます：

```bash
npx create-next-app@latest neowhisper-blog
```

以下のオプションを選択してください：

- **TypeScript?** Yes（必須です）
- **Tailwind CSS?** Yes（2026年の標準です）
- **App Router?** Yes
- **Import Alias?** Yes (`@/*`)

</Step>

<Step number="2" title="MDX サポートの追加">

Markdownの手軽さとReactのパワーを両立させます。必要なパッケージをインストールします：

```bash
npm install @next/mdx @mdx-js/loader @mdx-js/react gray-matter date-fns
npm install -D @types/mdx
```

次に `next.config.js` を設定します：

```js
import createMDX from "@next/mdx";

/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ["js", "jsx", "md", "mdx", "ts", "tsx"],
};

const withMDX = createMDX({
  options: {
    remarkPlugins: [],
    rehypePlugins: [],
  },
});

export default withMDX(nextConfig);
```

そして、ルートディレクトリに `mdx-components.tsx` を作成します：

```tsx
import type { MDXComponents } from "mdx/types";

export function useMDXComponents(components: MDXComponents): MDXComponents {
  return {
    h1: ({ children }) => (
      <h1 className="text-4xl font-bold mb-4">{children}</h1>
    ),
    h2: ({ children }) => (
      <h2 className="text-3xl font-bold mb-3 mt-8">{children}</h2>
    ),
    a: ({ href, children }) => (
      <a href={href} className="text-purple-600 hover:underline">
        {children}
      </a>
    ),
    ...components,
  };
}
```

</Step>

<Step number="3" title="コンテンツの構造化">

コードは綺麗に保ちましょう。記事は `src/content/posts` に保存します。

```text
src/
├── app/              # ルーティング
├── components/       # UIコンポーネント
├── content/          # MDXファイル
│   └── posts/
│       └── first-post.mdx
└── lib/              # ユーティリティ
    └── posts.ts
```

最初の記事 `src/content/posts/first-post.mdx` を作成します：

```mdx
---
title: "初めての投稿"
date: "2026-01-13"
excerpt: "Hello world!"
coverImage: "/images/first-post.png"
---

# Hello World

これが最初の投稿です。
```

</Step>

<Step number="4" title="データレイヤーの構築">

`src/lib/posts.ts` を作成してファイルを読み込みます。App Routerでは、単純にデータをフェッチするだけで済みます。複雑な状態管理は不要です。

```tsx
import fs from "fs";
import path from "path";
import matter from "gray-matter";

const postsDirectory = path.join(process.cwd(), "src/content/posts");

export type Post = {
  slug: string;
  title: string;
  date: string;
  excerpt: string;
  coverImage: string;
  content: string;
};

export async function getAllPosts(): Promise<Post[]> {
  const fileNames = fs.readdirSync(postsDirectory);
  const allPostsData = fileNames
    .filter((fileName) => fileName.endsWith(".mdx"))
    .map((fileName) => {
      const slug = fileName.replace(/\.mdx$/, "");
      const fullPath = path.join(postsDirectory, fileName);
      const fileContents = fs.readFileSync(fullPath, "utf8");
      const { data, content } = matter(fileContents);

      return {
        slug,
        content,
        title: data.title,
        date: data.date,
        excerpt: data.excerpt,
        coverImage: data.coverImage,
      } as Post;
    });

  return allPostsData.sort((a, b) => (a.date < b.date ? 1 : -1));
}

export async function getPostBySlug(slug: string): Promise<Post | null> {
  try {
    const fullPath = path.join(postsDirectory, `${slug}.mdx`);
    const fileContents = fs.readFileSync(fullPath, "utf8");
    const { data, content } = matter(fileContents);

    return {
      slug,
      content,
      title: data.title,
      date: data.date,
      excerpt: data.excerpt,
      coverImage: data.coverImage,
    } as Post;
  } catch {
    return null;
  }
}
```

</Step>

<Step number="5" title="UI の接続">

`src/app/page.tsx` で記事を取得して表示します：

```tsx
import { getAllPosts } from "@/lib/posts";
import ArticleCard from "@/components/ArticleCard";

export default async function Home() {
  const posts = await getAllPosts();

  return (
    <main className="container mx-auto px-4 py-12">
      <h1 className="text-4xl font-bold mb-8">最新の投稿</h1>
      <div className="grid md:grid-cols-2 gap-8">
        {posts.map((post) => (
          <ArticleCard key={post.slug} post={post} />
        ))}
      </div>
    </main>
  );
}
```

個別記事 (`src/app/blog/[slug]/page.tsx`) は、ビルド時に静的に生成 (`generateStaticParams`) します：

```tsx
import { getPostBySlug, getAllPosts } from "@/lib/posts";
import { notFound } from "next/navigation";
import { MDXRemote } from "next-mdx-remote/rsc";

export async function generateStaticParams() {
  const posts = await getAllPosts();
  return posts.map((post) => ({ slug: post.slug }));
}

export async function generateMetadata({
  params,
}: {
  params: { slug: string };
}) {
  const post = await getPostBySlug(params.slug);
  if (!post) return { title: "Post Not Found" };
  return { title: post.title, description: post.excerpt };
}

export default async function BlogPost({
  params,
}: {
  params: { slug: string };
}) {
  const post = await getPostBySlug(params.slug);
  if (!post) notFound();

  return (
    <article className="max-w-3xl mx-auto py-12 px-4">
      <h1 className="text-5xl font-bold mb-4">{post.title}</h1>
      <time className="text-gray-500 block mb-8">{post.date}</time>
      <div className="prose lg:prose-xl dark:prose-invert max-w-none">
        <MDXRemote source={post.content} />
      </div>
    </article>
  );
}
```

</Step>

---

## 次のステップ

<Checklist>
  <CheckItem>**ダークモード**: `next-themes` を追加。</CheckItem>
  <CheckItem>
    **検索機能**: `fuse.js` でクライアントサイド検索を実装。
  </CheckItem>
  <CheckItem>
    **コードハイライト**: `rehype-pretty-code` か `rehype-highlight` を追加。
  </CheckItem>
  <CheckItem>**SEO**: `sitemap.xml` と `robots.txt` を追加。</CheckItem>
  <CheckItem>**デプロイ**: Vercel にプッシュして公開。</CheckItem>
</Checklist>

---

**Next.jsプロジェクトの構築でお困りですか？** NeoWhisperでは、モダンなWeb開発を専門としています。[サービス詳細](/services?lang=ja)をご覧いただくか、[お問い合わせ](/contact?lang=ja)からご相談ください。
